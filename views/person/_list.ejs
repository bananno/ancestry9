<% if (locals.format == 'bullet') { %>
  <ul class="bullet-list">
    <% people.forEach(person => { %>
      <li>
        <%- include('./_link', {person}) %>
      </li>
    <% }); %>
  </ul>
<% } else if (locals.format == 'list') { %>
  <ul class="no-bullet-list">
    <% people.forEach(person => { %>
      <li>
        <%- include('./_link', {person}) %>
      </li>
    <% }); %>
  </ul>
<% } else { %>
  (specify peopleList format)
<% } %>
